# AutoML-Prediction-Tool
A full-stack AutoML tool (FastAPI + JS) for classification and churn prediction. It automatically cleans any CSV, handles missing data/outliers, and trains four models (LR, RF, XGB, NN) with GPU support for instant predictions.
